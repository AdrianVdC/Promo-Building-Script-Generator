# NATS Bonus Creator — Changelog

| Version | Date | Summary |
|---|---|---|
| v2.30 | Aug 20, 2026 | **DMCA Free Spins days unlocked** — Playmaker Bonus Spins is now functional, so Mon/Wed/Fri/Sun Daily Missions generate end-to-end; DMCA is complete across all seven weekdays. **(1) FS day cards active** — the amber lock notice is gone; FS days render four **Bet → Spins** tier checkboxes with that weekday's Terms IDs inline ($50/20 spins @ $0.10, $125/50 @ $0.10, $500/50 @ $0.50, $5,000/100 @ $3.00; Mon `CAS_CA_0043`–`0046`, Wed `0047`–`0050`, Fri `0051`–`0054`, Sun `0055`–`0058`), Days to Use Reward (default 7), and FS toast/image notes; FS state lives in a new `dmcaFSOfferData` array separate from CC state, so a date edit that flips the weekday never mixes tiers. **(2) Stakes ladders + two-layer validation** — `DMCA_GAMES` gains per-game `search` strings and **Free Spin Stakes ladders captured from PROD Playmaker** (BAA/4CC/BWZ: full 32-step ladder 0.10→100.00; **MFB caps at 50.00**, 27 steps; all four contain the tier values 0.10/0.50/3.00). Generation-time: a tier whose spin value is not in the weekday game's ladder shows a red `not in {token} stakes` row warning and **blocks generation with an alert**; runtime: the script dumps the live stakes options and **hard-fails, refusing to substitute**, if the exact value is absent — the T&C and Terms ID legally state the spin value, so a nearby value is never selected. **(3) FS Playmaker Step 4** — Bonus Spins radio (`button[role='radio'][value='free_spin']`) → Number of Spins (`#fs-count`) → Aggregator **PLAYTECH** → Provider **PLAYTECH** (Radix comboboxes located by aria-label substring) → Casino Game selected with ™-tolerant matching (™/® stripped, whitespace collapsed, substring on the pre-™ `search` string; actual option text printed) → **Free Spin Stakes** combobox located inside `[data-testid='free-spin-stakes-grid']` (⚠️ it has **no aria-label**; the grid **offers CAD for Canada** — the v2.28 USD concern is resolved) → Days to Use Reward. **(4) 00:01 bonus times for ALL DMCA builds** — Playmaker start/end times moved from 00:00 to **00:01** (12:01 AM typed date → 12:01 AM next day), CC days included, per stakeholder direction; the fill uses the native-setter first with a read-back, then a keyboard retype, then a `fill()`+Tab fallback, printing the final value either way (the native-setter alone was observed to silently fail). The NATS promo window is unchanged (typed date 00:00:00 → 23:59:59 ET). **(5) FS copy baked in** — FS HIW (`We'll reward you with {spins} Bonus Spins to use on the slots game {game_name}`) and one FS T&C template with dynamics `{bet_fmt}` / `{spins}` / `{spin_value}` (two-decimal) / `{game_name}` / `{terms_id}` / `{start_date_long}` / `{end_date_long}`, title line `FANATICS CASINO (CANADA) – BET {bet_fmt} CAD, GET {spins} BONUS SPINS ({terms_id})`; **byte-verified against all sixteen FS filings (CAS_CA_0043–0058)** with two stakeholder-approved normalizations: exactly one space always follows `{game_name}` (correcting the Monday filings' glued \"Baa Baa Baa™and\" defect ×3 spots each, Wed 0047/0048, and the double-space defect in Fri 0053 §10 and Sun 0055 §5/§10) and trailing whitespace stripped (Wed 0049/0050, all Friday incl. 0054, all Sunday §4); the §5 \"$15 CAD\" winnings example is static on all tiers; all four FS days share the CC promotional window (08/17/26 → 12/31/26), baked at generation time. **(6) FS toast** — `Promotion Complete!` / `You have been awarded {spins} Bonus Spins to {game_name}` — **no quotes around the game name** (stakeholder-confirmed; supersedes the quoted variant recorded in v2.28). **(7) FS images activated** — Discover `{Weekday}_B{bet}_G{spins}S_{value}V.png` with the spin value in cents (e.g. `Wednesday_B50_G20S_10V.png`); same weekday-prefixed Promo Detail/Masthead; pre-flight file check covers FS files. **(8) Resilient Phase 2** — each Playmaker bonus now runs in a per-bonus try/except: a failure (e.g. the stakes hard-fail) prints `FAILED {code}`, recovers by navigating back to Bonus Setup, and continues; a **SAVED/FAILED results summary** prints at the end. **(9) Step 2 clarification** — the Playmaker segment field is a **\"Create Account Segments\"** field: typing a name + Enter creates a chip that **creates the segment on Save Bonus** (any new name works; no pre-existing segment needed) — earlier docs' \"chip attaches existing segment\" wording corrected. `CODES` entries gain `reward` / `spins` / `spin_value` / `vtok` / `game_name` / `game_search` fields (empty on CC codes); generated-script header updated. **PROD validation (Aug 14–19, 2026)** — iterative dry runs captured the FS selectors and all four stakes ladders; full FS flows built live end-to-end with Save Bonus clicked: `120426` MFB ×2 tiers, `121626` 4CC, and a live same-day `081926` 4CC $50 build confirmed in-app — all test builds to be deleted from PROD (incl. the v2.28-era `120126` CC promo). **Verification** — per the template-literal limitation: `buildDMCAScript` rendered through a real JS engine (1 CC + 2 FS codes across three games), resulting Python passed `ast.parse` + `py_compile`, **43/43 content checks** — FS T&C **byte-identical** to the PROD-validated test-script template, CC HIW/T&C/images regression, FS image names incl. cents token, 00:01 fill with no 00:00 remnant, regex/`™`-strip escapes surviving JS-literal rendering, toast variants, results summary, Okta launch unchanged — plus **10/10 sandboxed UI checks** through the real `generateScript()` (FS no-offers/bad-stake/missing-Terms-ID all alert-block, old lock alert gone, FS/CC/mixed end-to-end generation producing compiling Python, weekday/leap-year pill logic, BWZ Sunday render). **Regression proof:** all 10 other script builders hash-identical to v2.29; zero functions added/removed; the whole-file diff is confined to DMCA branches and the title bump, so every other offer type generates byte-identical scripts. The harness caught a template-literal backtick-in-docstring bug before ship (limitation #5 class). **FS-day DMCA scripts must be generated from v2.30+** (first version to support them); **regenerate any pending DMCA CC scripts too** — v2.28/v2.29 scripts set Playmaker bonus times to 00:00 rather than 00:01. |
| v2.29 | Aug 19, 2026 | **LC-CHURN-DM — Matched Deposit always checked** — the Churn DM bonus flow now checks the **Matched Deposit** checkbox on every bonus. A new step was inserted into the Churn DM `create_bonus` immediately after the Entitlement Type `Deposit` radio click and before the summary percentage fill; the block is copied verbatim from the production-confirmed VIPDM implementation, including the explicit 10-second visibility wait (the checkbox lives inside the DEPOSIT entitlement panel, which only renders after the Entitlement Type radio is set). Terminal output prints `OK Matched Deposit: checked`. All other Churn DM behavior is unchanged (Casino Credit type, `Casino Credit (%)` checkbox, `Days to Meet Casino_credit Settlement`, standard platform tags, 10-day window, no confirmation modal). The in-tool Instructions panel for Churn DM now documents the always-checked Matched Deposit. Also fixed: the page `<title>` had drifted to `v2.27` in the v2.28 release (display-only; no functional impact) — corrected to `v2.29`. **Verification** — per the template-literal limitation: the inserted block contains no backslashes, backticks, or `${` (identity under template-literal rendering by construction) and was additionally round-tripped through a real JS template literal via node with the resulting Python passing `ast.parse`; the whole-file diff vs v2.28 contains exactly three edits (the title bump, the Instructions-panel bullet, and the six-line Matched Deposit block inside the Churn DM `create_bonus`), builder-confirmed by voucher prefix (`CRLCDM`); the VIPDM builder's own Matched Deposit block and all other builders are untouched. Confirmed working in a live NATS run Aug 19, 2026. **Churn DM scripts must be generated from v2.29+** — scripts from v2.28 and earlier leave Matched Deposit unchecked; regenerate all pending Churn DM scripts. |
| v2.28 | Aug 14, 2026 | **New offer type: Daily Missions - Canada (DMCA)** — twelfth offer type; daily wager-and-get missions for Ontario, forked from the BG-RE two-phase architecture and pointed at **production** for both systems (NATS `trading.1.betfanatics.com`, Playmaker `playmaker-internal.1.betfanatics.com/home` — the first offer type to use production Playmaker). **(1) Weekday-driven model** — the typed date's weekday determines everything (no reward-type/game dropdowns): **Tue/Thu/Sat = Casino Credit** (shared T&C set), **Mon/Wed/Fri/Sun = Free Spins**, each FS day with its own game (Mon Baa Baa Baa™/BAA, Wed 4 Crazy Cluckers™/4CC, Fri Mega Fire Blaze™: Legacy of the Tiger™/MFB, Sun Blue Wizard: Cash Collect & Link™/BWZ) and its own Terms ID set. A pill next to the date input shows the derived weekday + reward type (+ game on FS days) live as the date is typed. **⚠️ FS days are LOCKED in v2.28** — Playmaker Bonus Spins setup is not yet working; FS-day cards render an amber lock notice with greyed tiers and block generation with an alert; only CC days generate. FS tiers ($50/20 spins @ $0.10, $125/50 @ $0.10, $500/50 @ $0.50, $5,000/100 @ $3.00) and Terms IDs (Mon `CAS_CA_0043`–`0046`, Wed `0047`–`0050`, Fri `0051`–`0054`, Sun `0055`–`0058`) are pre-configured for the unlock. **(2) Day cards** — 3 default cards; CC-day state shows four **Bet → Get** tier checkboxes with inline Terms IDs ($50→$2 `CAS_CA_0019`, $125→$5 `CAS_CA_0020`, $500→$25 `CAS_CA_0021`, $5,000→$300 `CAS_CA_0022`), Days to Use Reward (default 7), and static notes (toast always ON; weekday image filenames); step badges **Promo + Bonus only** (BG-RE style — the segment is attached inside Playmaker by code name). Region row and Edit HIW / Edit T&Cs buttons hidden (copy baked in; changes require Adrian); Edit Images visible with new `daily_missions_image_path` key (default `.../Marketing Automations/Bau Automations/Daily Missions`, Drive `folders/1w-o2bP7em8fkT86lwOtCu66jO57igRLh`). **(3) Naming** — CC code `MMDDYY_CAS_RET_BG_CA_CC_B{bet}_G{get}` (shape-identical to a standard BG CA code by stakeholder acceptance; FS reserved shape `MMDDYY_CAS_RET_BG_CA_FS_B{bet}_G{spins}S_{value}V_{game}` with spin value in cents); identical across NATS and Playmaker; **no voucher codes** (bonus lives in Playmaker). Script filename `daily_missions_MMDDYY_HHMM.py`. **(4) Windows** — NATS promo: **typed date 00:00:00 → 23:59:59 ET (single-day — a new window pattern)**; Playmaker bonus: 00:00 typed date → 00:00 next day (BG-RE shape). **(5) NATS promo phase** — Image only CTA / Overlay, Title & Promo Header auto-derived `{Weekday}'s Daily Mission`, Bonus Tile ON, Button 2 empty, no confirmation modal; images from the local Drive folder with **underscored, weekday-prefixed filenames** auto-derived from the date: `{Weekday}_Promo_Detail.png`, `{Weekday}_Masthead.png`, discover `{Weekday}_B{bet}_G{get}.png`; a pre-flight check aborts before login if any required file is missing. HIW (no "CAD" by design) and the full Ontario CC T&C are baked in — one legal template with `{bet_fmt}` / `{get_fmt}` / `{terms_id}` / `{start_date_long}` / `{end_date_long}` (window 08/17/26 → 12/31/26 baked); §5's $2/$20/$5 worked examples and the "each time you are eligible" clauses in §4/§7 are static legal copy; byte-verified against the supplied CAS_CA_0019/0020 filings with two approved normalizations (space after `{game_name}` in the FS template; trailing whitespace stripped). ⚠️ CAS_CA_0021/0022 and all FS-day filings were not supplied — verify on first use. **(6) Playmaker bonus phase** — Steps 1–6 with **Save Bonus clicked**: Canada jurisdiction (`#jurisdiction-ontario`), segment chip = code name (confirmed attaching), wagering = bet, **Casino Credit radio** (`value='casino_credit'`), settlement = get, Days to Use Reward from the card, promo selected by code name in Step 5, **toast always ON** (`Promotion Complete!` / `You have been awarded $X Casino Credit`; FS variant `You have been awarded X Bonus Spins to "GAME NAME"` recorded for the unlock). **(7) Okta Verify (FastPass) login fix** — both phases launch a **persistent Chromium profile** (`~/.dm_canada_pw_profile`) with `--disable-features=LocalNetworkAccessChecks` and a `local-network-access` permission grant for `betfanatics.okta.com`, resolving Playwright's auto-denied permission prompt that blocked Okta device-app access — confirmed working in PROD; candidate for back-porting to other offer types. **(8) PROD validation (Aug 14, 2026)** — NATS promo `120126_CAS_RET_BG_CA_CC_B125_G5` built live end-to-end via the one-time validation script (Tuesday images, single-day window, CAS_CA_0020 T&C verbatim, Bonus Tile ON); Playmaker dry run filled Steps 1–6 in production (Canada, CC radio, wagering 125, settlement 5, promo + segment chip attached) with Save Bonus located but not clicked — "worked perfectly." ⚠️ That test promo still exists in PROD NATS; delete it or avoid reusing `120126` B125_G5 to prevent a duplicate-name collision. **Verification** — per the template-literal limitation: `buildDMCAScript` rendered through a real JS engine, resulting Python passed `ast.parse` + `py_compile`; **31/31 content checks** (prod URLs, per-code fields, T&C formatting incl. `$5,000` comma formatting, static §5 examples intact, 14 numbered sections, no leftover placeholders, no trailing whitespace, HIW byte-exactness) plus **43/43 sandboxed UI checks** through the real `onOfferTypeChange()` handler (weekday/game pill logic incl. leap-year and invalid-date edges, FS lock render + generation block, CC end-to-end generation producing valid Python, badge toggle → `do_bonus False`, resetDay, region/HIW/T&C buttons hidden, image path default, and all 11 other offer types still switching cleanly). **Regression proof:** all nine other script builders are **byte-identical to v2.27** (hash-compared); the RTC inline builder text untouched; every shared-function change is a DMCA-guarded branch appended to an existing chain (the only unconditional additions are two inert day-object fields in `initDays`/`resetDay` that no other type reads). **DMCA scripts must be generated from v2.28+** (first version to include the offer type). |
| v2.27 | Aug 14, 2026 | **VIPBG CA — category-specific Masthead & Promo Detail creatives** — CA — Canada Slots and Tables offers now use different Masthead and Promo Detail images. In the CA-rendered script, `create_promo` derives the category word from the code's `SBG`/`TBG` token via the existing `GAME_CATEGORIES` map and uploads **`Slots Promo Detail.png` + `Slots Masthead.png`** for Slots offers and **`Tables Promo Detail.png` + `Tables Masthead.png`** for Tables offers (Discover filename unchanged: `{SBG\|TBG}_B{bet}_G{get}.png`). The four new files must exist in the CA image folder (`.../VIP Automations/Offer Library/Canada/Bet & Gets`); the generic `Promo Detail.png` / `Masthead.png` are no longer referenced by CA VIPBG scripts. **US VIPBG is unchanged** — still generic `Promo Detail.png` / `Masthead.png` + `B{bet}_G{get}.png`. No other offer type touched. In-tool Instructions panel updated to list the region-specific filename sets. Verified per the template-literal limitation: `buildVIPBGScript` extracted verbatim, rendered through a real JS engine for both regions, resulting Python compiled and executed — CA SBG/TBG codes assert the correct category filenames and Discover names, US asserts the generic filenames with no `category_word` machinery; whole-file diff vs v2.26 contains only the title bump, the Instructions bullet, and the CA/US filename block inside VIPBG `create_promo` (all other builders byte-identical). |
| v2.26 | Aug 12, 2026 | **VIPDM US — United States region** — VIP Offer Library - Deposit Matches is now dual-region (mirror of the VIPBG v2.25 pattern): CA — Canada keeps its base `vcl_dm_*` localStorage keys and behavior; US layers parallel `_us` keys via new `isVIPDMUS()` / `vipdmKey(base)` helpers. **(1) Region** — the VIPDM Region dropdown now offers **US / CA** and **always defaults to US when the offer type is selected** (per stakeholder direction; unlike VIPBG, which keeps a valid current selection and defaults CA — no other offer type's default behavior was modified); switching region calls `resetAll()` silently (dates, selected offers, toggles cleared; generated script hidden) so US and CA inputs can never mix — same rule as RTC CC / VIPBG. **Visual cue:** the edit buttons render in the existing `body.ca-region` red shades when CA is selected and default blue for US (`updateRegionTheme()` extended with a VIPDM-CA condition that is inert for every other offer type). **(2) US offer model** — reward is **FanCash in USD** (not Casino Credit/CAD); **ten** default tiers unselected, keyed `M{min}_{pct}M{max}`: $100/50%/$250 → `CAS_10685`, $100/100%/$250 → `CAS_10686`, $250/10%/$500 → `CAS_10677`, $250/10%/$1,000 → `CAS_10678`, $250/10%/$2,500 → `CAS_10679`, $250/10%/$5,000 → `CAS_10680`, $250/20%/$500 → `CAS_10681`, $250/20%/$1,000 → `CAS_10682`, $250/20%/$2,000 → `CAS_10683`, $250/20%/$5,000 → `CAS_10684` (relative to CA's nine: adds 10%/$500 and 10%/$1,000, drops 10%/$250); default US promotional period **07/01/26 → 12/31/26**. **(3) Naming** — US code `MMDDYY_VCL_RET_DM_US_FC_M{min}_{pct}M{max}` (the `_US_` token routes `fill_currency_amount()` to the **USD row** for the reward and Minimum/Maximum Deposit cards; `FC` = FanCash); CA code unchanged. **US voucher `VRDMFC{MMDDYY}{XXX}`** (CA keeps `VRDMCC`). Window unchanged both regions: the month-anchored model (typed date − 1 day `00:00:00 ET` → month-end `23:59:59 ET` + Days to Meet Entitlement). **(4) US generated-script differences** — bonus type **FanCash** with the standard-DM matched-deposit form: `Bonus (%)` checkbox (CA: `Casino Credit (%)`), `Days to Meet Fancash Settlement` (CA: `Days to Meet Casino_credit Settlement`); platform tags follow the **standard** pattern — only the two Sportsbook tags removed, **`COMBO: Casino` kept** (CA keeps its three-tag removal; CA VIPDM and CA VIPBG remain the only flows that remove `COMBO: Casino`); **defensive confirmation-modal handler added US-only** — a FanCash modal is **expected** (standard FanCash DM shows one); it is confirmed if it appears and its absence prints an "unexpected — verify" terminal note; **the CA-rendered script contains no modal block and is functionally byte-identical to v2.25** (one comment line only — CA's production-confirmed no-modal flow is untouched). Everything shared stays shared: `From Your VIP Host` title/header/description, **Button 2 `Deposit` → `/auth/account/deposit`**, Send To XP default ON / External Bonus default OFF (toggles retained), **Matched Deposit always checked**, `{pct}M{max}.png` Discover filenames, Minimum Deposit from `M{min}` / Maximum Deposit 1000000, Series `VIP` / Type `Retention` (visible-dropdown scoping), Origin `Opt-In Bonus`, Reporting CAS, Days 3/3/7, Terms ID enforcement blocking generation. **(5) US copy** — HIW (region-scoped, saved `vcl_dm_hiw_us`): `1. Opt-in to the promotion / 2. Make a single deposit of {min_fmt} or more / 3. We'll instantly match your deposit {pct}%, up to {max_fmt} FanCash`. US T&C is a **single canonical template** derived from the CAS_10677 legal filing; the three supplied filings (CAS_10677 / CAS_10678 / CAS_10686) differed by two copy-edit inconsistencies, normalized by stakeholder decision — "Fanatics Sportsbook and **Casino App**" (the CasinoApp missing-space defect in 10678/10686, same class as VIPBG US) and "**Only** your first Qualifying Deposit" (majority wording; 10686 said "Your first") — plus two global normalizations: all trailing whitespace stripped and the title line prefixed `FANATICS CASINO - `, giving `FANATICS CASINO - {min_fmt}+ DEPOSIT, {pct}% MATCH IN FANCASH (MAX {max_fmt}) (ID: {terms_id})`. Everything else is verbatim legal copy (orphaned "(ii)", "apply- see fanatics.com", double spaces, curly quotes preserved). Dynamic fields `{min_fmt}` / `{pct}` / `{max_fmt}` / `{terms_id}` / `{start_date_long}` / `{end_date_long}`. The dynamic-field chip bar shows a US-specific set when US is selected. **(6) UI/storage** — card title, Edit T&Cs / Edit Terms Expiry & Terms IDs / Edit Images modal titles, and the Drive link are all region-aware (US Drive `folders/1W8GLxrZFBMotLP_WPzAE04KPPb__BxIX`, default path `.../VIP Automations/Offer Library/USA/Deposit Matches`); six new keys `vcl_dm_hiw_us`, `vcl_dm_tc_us`, `vcl_dm_image_path_us`, `vcl_dm_terms_ids_us`, `vcl_dm_terms_start_date_us`, `vcl_dm_terms_end_date_us`, all cleared by Clear Saved Settings. ⚠️ US Discover filenames have one-character near-collisions: `10M500` / `10M5000` and `20M500` / `20M5000` — double-check the creatives. **Verification** — full harness per the template-literal limitation: `buildVIPDMScript` rendered through a real JS engine for **US and CA × 6 date edge cases (12 scripts)**, each compiled and executed — **692 automated checks** covering parser round-trips, Terms ID resolution, USD/CAD routing, `VRDMFC`/`VRDMCC` voucher format, window math (incl. same-month same-end, Jan-1 prior-month start, leap-Feb 2028, Dec year-end + entitlement crossover), region-specific structural selectors (FanCash vs Casino Credit forms, COMBO lists, settlement labels, modal block presence/absence), HIW byte-exactness, and **byte-level comparison of rendered US T&Cs against all three supplied legal filings** (with the approved normalizations applied) — plus **35 sandboxed UI checks** (region defaulting, key scoping, per-region defaults/dates/images, theme toggling, Clear Saved Settings). **Regression proof:** all nine other builders are **byte-identical** to v2.25 (hash-compared), the whole-file diff contains only VIPDM-related lines, and CA VIPDM T&C/HIW/voucher outputs are identical to v2.25. ⚠️ CAS_10679–CAS_10685 legal filings (seven of ten) were not supplied — the canonical template is assumed to cover them; eyeball on first use. First live US run should confirm the FanCash confirmation modal fires. **US VIPDM scripts must be generated from v2.26+** — earlier versions were CA-locked. |
| v2.25 | Aug 12, 2026 | **VIPBG US — United States region** — VIP Offer Library - Bet & Gets is now dual-region (mirror of the RTC CC v2.22 pattern): CA — Canada keeps its base localStorage keys and behavior; US layers parallel `_us` keys via new `isVIPBGUS()` / `vipbgKey(base)` helpers. **(1) Region** — the VIPBG Region dropdown now offers **US / CA** (keeps the current selection if valid, otherwise defaults to CA, the original region); switching region calls `resetAll()` silently (dates, selected offers, toggles cleared; generated script hidden) so US and CA inputs can never mix — same rule as RTC CC. **Visual cue:** the three edit buttons render in the existing `body.ca-region` red shades when CA is selected and default blue for US (reuses RTC CC's theme class; `updateRegionTheme()` extended with a VIPBG-CA condition that is inert for every other offer type). **(2) US offer model** — reward is **FanCash in USD** (not Casino Credit/CAD); **no Slots/Tables differentiation** — all US tiers are slots B&Gs, so US offer rows are **Bet → Get only** (no category dropdown), keyed `B{bet}_G{get}` with no prefix; four defaults unselected ($1,000/$100 → `CAS_10687`, $5,000/$500 → `CAS_10688`, $20,000/$1,000 → `CAS_10689`, $100,000/$5,000 → `CAS_10690`); default US promotional period **07/01/26 → 12/31/26**. **(3) Naming** — US code `MMDDYY_VCL_RET_BG_US_FC_B{bet}_G{get}` (the `_US_` token routes `fill_currency_amount()` to the **USD row**; `FC` = FanCash); CA code unchanged. Voucher unchanged both regions: `CLBGFC{MMDDYY}{XXX}`. Window unchanged both regions: the VIPDM/VIPBG month-anchored model (typed date − 1 day `00:00:00 ET` → month-end `23:59:59 ET` + Days to Meet Entitlement). **(4) US generated-script differences** — bonus type **FanCash** (flat amount = "get", `Days to Meet Fancash Settlement`, `FanCash Amount` label, USD row); **Button 2 filled `Play Now!` → `/docs/usered/casgenericgamelist`** (the values previously reserved in the CA script header); platform tags follow the **standard** pattern — only `COMBO: Sportsbook` + `COMBO: Sportsbook And Casino` removed, **`COMBO: Casino` kept** (CA keeps its three-tag removal; CA VIPBG and VIPDM remain the only flows that remove `COMBO: Casino`); no `GAME_CATEGORIES` machinery (`parse_vipbg` returns `(bet, get)`); conditional Stake Chunk Sizes fill retained; defensive confirmation-modal handler retained — for US a modal is **expected** (standard FanCash BG shows one), and its absence prints an "unexpected — verify" terminal note. Everything shared stays shared: `From Your VIP Host` title/header/description, Send To XP + External Bonus always ON, Series `VIP` / Type `Retention` (visible-dropdown scoping), Origin `Opt-In Bonus`, Reporting CAS, Days 3/3/7, Terms ID enforcement blocking generation. **(5) US copy** — HIW (region-scoped, unlike RTC CC where HIW is shared): `1. Opt-in to the promotion / 2. Wager {bet_fmt}+ on any of your favorite Slot games / 3. I'll instantly give you {get_fmt} in FanCash` (US spelling "favorite"; "Slot games" static — no `{game_category}` field). US T&C is a **single canonical template** built from the CAS_10687/CAS_10688 legal filings, which were **not** word-identical — four copy-edit inconsistencies were found and normalized by stakeholder decision: "Fanatics Sportsbook and Casino App" (space fixed), "wagering **requirements**" (plural), single period in Limitations, and "Fanatics Casino aims to **consistently care** for our customers and promote responsible gambling practices." Dynamic fields `{bet_fmt}` / `{get_fmt}` / `{terms_id}` / `{start_date_long}` / `{end_date_long}` (no game-category fields); title line `FANATICS CASINO - VIP BET {bet_fmt} GET {get_fmt} FANCASH (ID: {terms_id})`; MI/NJ/PA/WV, 21+, FBG Enterprises Opco LLC, 72-hour wagering language, 7-day FanCash expiry, and the fanatics-one.com/fancashterms URL are static legal copy; curly quotes preserved. The dynamic-field chip bar shows a US-specific set when US is selected. **(6) UI/storage** — card title, offers label, Edit T&Cs / Edit Terms Expiry & Terms IDs / Edit Images modal titles, and the Drive link are all region-aware (US Drive `folders/13neRGeYM4JLa98p827vbepp-eKu2_hXq`, default path `.../VIP Automations/Offer Library/USA/Bet & Gets`, Discover filenames `B{bet}_G{get}.png` — full offer key, no category prefix); six new keys `vcl_bg_hiw_us`, `vcl_bg_tc_us`, `vcl_bg_image_path_us`, `vcl_bg_terms_ids_us`, `vcl_bg_terms_start_date_us`, `vcl_bg_terms_end_date_us`, all cleared by Clear Saved Settings. **Verification** — full harness re-built per the template-literal limitation: `buildVIPBGScript` extracted verbatim, rendered through a real JS engine for **US and CA × 6 date edge cases (12 scripts)**, each compiled and executed — **742 automated checks** covering parser round-trips, Terms ID resolution, USD/CAD routing, voucher format, window math, always-ON flags, region-specific structural selectors (FanCash vs Casino Credit, Button 2, COMBO lists), HIW byte-exactness, T&C byte-exactness vs the baked templates, the four canonical-wording assertions, and **byte-level comparison of rendered US T&Cs against both legal source documents** (with the agreed fixes applied). **Regression proof:** all nine other builders are **byte-identical** to v2.24 (hash-compared), `generateScript` changed only inside its VIPBG branch, the CA-rendered VIPBG script is functionally byte-identical to v2.24 (two comment lines only), and the theme change is provably inert for non-VIPBG offer types. ⚠️ CAS_10689 / CAS_10690 legal filings were not supplied — the canonical template is assumed to cover them; eyeball on first use. First live US run should confirm the FanCash confirmation modal fires and whether Stake Chunk Sizes exists on the FanCash form (script handles both outcomes). **US VIPBG scripts must be generated from v2.25+** — v2.24 cannot produce them (region was locked to CA). |
| v2.24 | Aug 12, 2026 | **New offer type: VIP Offer Library - Bet & Gets (VIPBG)** — eleventh offer type; monthly VIP-library wager-and-get offers paid in **Casino Credit**, **CA — Canada only** (Region selector locked to CA; US region planned, RTC CC-style). **(1) Single card** — one full-width card ("VIP Offer Library — Bet & Gets — CA — Canada"), `+ Add Day` is a no-op; eight **Category (Slots/Tables) → Bet → Get** tier rows with a per-row category dropdown and per-offer checkbox, all unselected by default (Slots: $1,000/$100, $5,000/$500, $20,000/$1,000, $100,000/$5,000; Tables: $2,000/$100, $10,000/$500, $25,000/$1,000, $100,000/$2,500), each row showing its Terms ID inline (`CAS_CA_0069`–`CAS_CA_0076`, keyed `{SBG\|TBG}_B{bet}_G{get}` — category prefix included as collision insurance); Segment/Promo/Bonus step badges; **Send to XP and External Bonus both ALWAYS ON — no toggles** (static note on the card; `SEND_TO_XP` / `EXTERNAL_BONUS` hardcoded `[True] * len(NAMES)`); Days to Opt In 3 / Entitlement 3 / Settlement 7. **(2) Window** — reuses the VIPDM month-anchored model verbatim: promo and bonus activation start `00:00:00 ET` one day before the typed date and end on the last day of the typed date's calendar month at `23:59:59 ET` plus the per-card Days to Meet Entitlement (`calendar.monthrange`; typed day anchors only the start; typed 1st starts in the prior month). **(3) Naming** — code `MMDDYY_VCL_RET_{SBG\|TBG}_CA_CC_B{bet}_G{get}` (SBG = Slots Bet & Get, TBG = Table Games Bet & Get; `RET` mirrors Type: Retention; `CA` token routes all amount fills to the CAD row); voucher `CLBGFC{MMDDYY}{XXX}` — shared with standard BG/BG-RE (NATS-mandatory but unused; the code-name category token distinguishes offers). **(4) Bonus model** — the wager requirement ("bet") is **enforced upstream by XP/segment logic and never entered on the bonus**; the bonus is a flat Casino Credit amount equal to the "get" value: Casino Credit type (no percentage checkbox, no deposit entitlement, no Matched Deposit), Days to Meet Casino_credit Settlement, **conditional Stake Chunk Sizes fill of `1`** (present on the flat CC form; skipped with a terminal note if absent), `fill_currency_amount` into the **CAD row**; platform tags reduced to **`Web` + `STAC: Standalone Casino` only** (all three COMBO tags removed, matching VIPDM — the two VIP Offer Library types are the only ones that remove `COMBO: Casino`); Reporting CAS (Casino), Origin `Opt-In Bonus`, **Series `VIP`** (typed search + anchored `^VIP$`), **Type `Retention`** (anchored `^Retention$`) — both scoped to `.ant-select-dropdown:not(.ant-select-dropdown-hidden)` with the same load-bearing Type scoping as VIPDM (hidden Bonus Origin dropdown contains a `Retention` option); Save uses **defensive confirmation-modal handling** (waits 5s; confirms if a modal appears, continues with a terminal note if not — production runs confirmed none appears, branch retained since standard FanCash BG does show one). **(5) Promo** — Title and Promo Header fixed `From Your VIP Host`; **Button 2 label/link intentionally left EMPTY for CA — Canada** (`Play Now!` → `/docs/usered/casgenericgamelist` documented in the generated script header, reserved for the future US region — changed after first live test); Bonus Tile ON; images from a locally-synced Drive folder (`vcl_bg_image_path`, default `.../VIP Automations/Offer Library/Canada/Bet & Gets`, Drive `folders/1tkMs1dx-gszSGzlBHxgMYvOYv2l-2UjO`; filenames `Promo Detail.png`, `Masthead.png`, `{SBG\|TBG}_B{bet}_G{get}.png`). **(6) Copy** — HIW is first-person VIP-host voice with Canadian spelling (`1. Opt-in to the promotion / 2. Wager {bet_fmt}+ on any of your favourite {game_category} games / 3. I'll instantly give you {get_fmt} in Casino Credit`; `{game_category}` = `Slots`/`Tables`; no trailer); T&C is a **single Canadian (Ontario) legal template covering both categories** — the Slots and Tables legal docs are word-for-word identical apart from dynamics, so one template with `{bet_fmt}` / `{get_fmt}` / `{terms_id}` / `{game_category_lower}` (`slots`/`table`, matching legal's asymmetric usage) / `{game_category_upper}` (`SLOTS`/`TABLES`) / `{start_date_long}` / `{end_date_long}` reproduces all 8 documents; title line `FANATICS CASINO (CANADA) – {game_category_upper} BET {bet_fmt} CAD, GET {get_fmt} CAD IN CASINO CREDIT ({terms_id})`; the seventy-two (72) hour wagering-window language, 7-day CC expiry, and §5 CAD examples are static legal copy (the 72h window is offer mechanics, independent of the month-anchored activation window); default promotional period 08/17/26 → 12/31/26. **(7) Terms modal** — the Edit T&Cs modal's Terms bar routes to a VIPBG **Edit Terms Expiry & Terms IDs** modal (start/end dates + one Terms ID per offer key); active offers without an ID show a red row warning and block generation. **(8) Storage** — `vcl_bg_hiw`, `vcl_bg_tc`, `vcl_bg_image_path`, `vcl_bg_terms_ids`, `vcl_bg_terms_start_date`, `vcl_bg_terms_end_date`; all cleared by Clear Saved Settings. Generates `vcl_bg_MMDDYY_HHMM.py`. Verified per the template-literal limitation: `buildVIPBGScript` extracted verbatim from the HTML, rendered through a real JS engine, and the resulting Python compiled and executed for all 8 tiers × 6 date edge cases (mid-month, typed month-end, leap-year Feb, non-leap Feb, Dec year crossover, typed Jan 1st) — **380 automated checks** asserting parser round-trips, Terms ID resolution, always-ON flags, voucher format, window math, HIW for both categories, and **byte-level verbatim comparison of rendered T&Cs against both legal documents**; the harness caught and fixed a `\$` template-escaping bug of the exact class behind the v2.20→v2.21 hotfix before it could ship. Confirmed end-to-end (segment + promo + bonus) in NATS production Aug 12, 2026. |
| v2.23 | Aug 10, 2026 | **New offer type: VIP Offer Library - Deposit Matches (VIPDM)** — tenth offer type; monthly VIP-library deposit-match offers paid in **Casino Credit**, **CA — Canada only** (Region selector locked to CA; a US region is planned, RTC CC-style). **(1) Single card** — one full-width card ("VIP Offer Library — CA — Canada"), `+ Add Day` is a no-op; nine **Min → % → Max** tier rows with per-offer checkbox, all unselected by default ($100/50%/$250, $100/100%/$250, $250/10%/$250, $250/10%/$2,500, $250/10%/$5,000, $250/20%/$500, $250/20%/$1,000, $250/20%/$2,000, $250/20%/$5,000), each row showing its Terms ID inline (`CAS_CA_0078`–`CAS_CA_0086`); Segment/Promo/Bonus step badges; Send to XP default ON; External Bonus default OFF; Days to Opt In 3 / Entitlement 3 / Settlement 7. **(2) Month-anchored window (new date model)** — promo and bonus activation start at `00:00:00 ET` **one day before the typed date** and end on the **last day of the typed date's calendar month at `23:59:59 ET` plus the per-card Days to Meet Entitlement** (`parse_dates(code, entitlement_days)` — first date function taking a per-card input; `calendar.monthrange` handles month lengths and leap years; typed day anchors only the start, so `080126` and `081726` share an end date; a typed 1st starts in the prior month — `010127` starts Dec 31, 2026, intended; changing entitlement changes both end dates). **(3) Naming** — code `MMDDYY_VCL_RET_DM_CA_CC_M{min}_{pct}M{max}` (VCL = VIP Casino Library; `RET` mirrors Type: Retention, not Origin; `CA` token routes all amount fills to the CAD row); voucher `VRDMCC{MMDDYY}{XXX}` (V+R+DM+CC, mirroring `CRDMFC` grammar). **(4) Promo** — Title and Promo Header fixed `From Your VIP Host`; **Button 2 `Deposit` → `/auth/account/deposit`** (reinstating the standard-DM button pattern Churn DM omits); Bonus Tile ON; images from a locally-synced Drive folder (`vcl_dm_image_path`, default `.../VIP Automations/Offer Library/Canada/Deposit Matches`, Drive `folders/1v9DEVtCNQPCgUkJsWtsXn7UJVIyBYtr4`; filenames `Promo Detail.png`, `Masthead.png`, `{pct}M{max}.png`). **(5) Copy** — HIW `{min_fmt}` / `{pct}` / `{max_fmt}` (no opt-in deadline line — the window bounds the offer; no trailer); T&C is the Canadian (Ontario) legal base (FBG Enterprises Canada, Inc., 19+/Ontario, ConnexOntario) with `{min_fmt}` / `{pct}` / `{max_fmt}` / `{terms_id}` / `{start_date_long}` / `{end_date_long}`, title line `FANATICS CASINO (CANADA) – DEPOSIT {min_fmt} CAD, GET {pct}% CASINO CREDIT (UP TO {max_fmt} CAD) ({terms_id})`; §1 "within 3 days" and §4 "during that Promo Period" are static by stakeholder direction; default promotional period 08/17/26 → 12/31/26. **(6) New Terms modal** — the Edit T&Cs modal's Terms bar routes to a VIPDM **Edit Terms Expiry & Terms IDs** modal (start/end dates + one Terms ID per offer key); Terms ID enforcement identical to Churn DM (active offers without an ID show a red row warning and block generation). **(7) Bonus** — Casino Credit + `Casino Credit (%)`; **Matched Deposit always checked** (inside the DEPOSIT entitlement panel, explicit visibility wait — added after live testing found it unchecked); **platform tags reduced to `Web` + `STAC: Standalone Casino` only** (all three COMBO tags removed — the only offer type that removes `COMBO: Casino`; also a live-testing fix); Minimum Deposit dynamic from `M{min}`, Maximum Deposit 1000000, all amount cards CAD row; Reporting CAS (Casino), Origin `Opt-In Bonus`, **Series `VIP`**, **Type `Retention`** — both selected via anchored regex scoped to `.ant-select-dropdown:not(.ant-select-dropdown-hidden)`; the Type scoping is load-bearing because the hidden Bonus Origin dropdown also contains a `Retention` option (page-wide exact-text would strict-mode crash); no confirmation modal. **(8) Storage** — `vcl_dm_hiw`, `vcl_dm_tc`, `vcl_dm_image_path`, `vcl_dm_terms_ids`, `vcl_dm_terms_start_date`, `vcl_dm_terms_end_date`; all cleared by Clear Saved Settings. Generates `vcl_dm_MMDDYY_HHMM.py`. Verified per the template-literal limitation: `buildVIPDMScript` extracted verbatim from the HTML, rendered through a real JS engine, and the resulting Python compiled and executed for all nine tiers plus edge cases (first-of-month, mid-month, Jan-1 year crossover, Feb leap year) — 143 automated checks asserting code names, CAD routing, parser round-trips, voucher format, window math, HIW/T&C field resolution, and selector/regex escaping survival; all eight pre-existing script builders confirmed **byte-identical** to v2.22, so no other offer type's generated output changed. |
| v2.22 | Aug 9, 2026 | **RTC CC Canada (CA) support** — RTC Top Up - Casino Credit is now fully dual-jurisdiction. **(1) Region dropdown restricted to US / CA** for RTC CC only (`updateJurisdictionOptions` RTC branch; defaults to US, falls back to US when switching from an offer type with a now-invalid region). All other offer types unchanged — RTC FS/RAF/SUO still MI/NJ/WV/PA; BG/DM/BG-RE/LC-REACT/LC-CHURN-DM still show all eight. **(2) Jurisdiction-scoped settings** via new `isRTCCanada()` / `rtcKey(base)` helpers: with CA selected, RTC CC settings read/write parallel `_ca` localStorage keys (`rtc_tc_ca`, `rtc_image_path_ca`, `rtc_terms_ids_ca`, `rtc_terms_start_date_ca`, `rtc_terms_end_date_ca`); US keys and behavior are byte-identical to v2.21. HIW is intentionally **shared** between US and CA (single `rtc_hiw` key, same `{amount_fmt}` field) per stakeholder direction. **(3) Canadian creative folder** — CA default image path `.../Lifecycle Automations/RTC Top Up - Canada`, same filename convention (`Promo Detail.png`, `Masthead.png`, `{amount}.png`); the Edit Images modal's Drive link switches to the Canadian folder (`https://drive.google.com/drive/folders/1W6ulHKv3ted9ZLNKzPgEbRoNCK_wFz6O`) when CA is selected. **(4) Canadian T&C base copy (Ontario)** baked in as the CA default — title line `FANATICS CASINO (CANADA) – CASINO CREDIT {amount_fmt} CAD SURPRISE DROP ({terms_id})`, FBG Enterprises Canada, Inc. entity, 19+ / Ontario eligibility, ConnexOntario responsible gaming language — using the same six dynamic fields as US (`{amount_fmt}`, `{terms_id}`, `{start_date_short/long}`, `{end_date_short/long}`). **(5) CA Terms IDs baked in:** `$2→CAS_CA_0001` through `$800→CAS_CA_0011` (all 11 tiers), with CA default promotional dates **08/17/26 → 12/31/26** stored separately from the US 06/01/26 → 12/01/26 window. **(6) Modals are jurisdiction-aware** — Edit T&Cs, Edit Terms Expiry & Terms IDs, and Edit Images read/save the selected region's values and display the region in the modal title (e.g. `Edit T&Cs — CA — Canada`). **(7) CA visual theme** — the three edit buttons shift from blues to matching red shades when CA is selected (`body.ca-region`: Edit HIW `#0C447C`→`#7C0C0C`, Edit T&Cs `#185FA5`→`#A51818`, Edit Images `#378ADD`→`#DD3737`); modal Save buttons share the classes and turn red too. **(8) Region change resets day cards** — switching Region in either direction on RTC CC calls `resetAll()` (dates, amount selections, and toggles cleared; script output hidden) so US and CA inputs can never mix; silent by design, no confirmation dialog. **(9) Clear Saved Settings** now clears all `_ca` keys. Generated CA scripts combine the existing v2.18 CAD-row amount fill (from the `_CA_` code token) with the CA image folder, CA T&Cs, and CA Terms IDs. Verified per the template-literal limitation: `format_tc` was extracted verbatim from the HTML, rendered through a real JS template literal with the CA copy and Terms IDs (including the `generateScript()` escaping pass), and the resulting Python executed for all 11 tiers — every tier asserted the correct title line, Terms ID, CAD amount, promotional dates, and Ontario-specific language. |
| v2.21 | Aug 9, 2026 | **Hotfix: v2.20 Casino Game regex broken by template escaping** — the v2.20 ™-tolerant game matcher was written with single backslashes (`\s`, `\(`, `\)`) inside the JS template literals that generate the Python scripts; JS template rendering swallows single backslashes, so generated scripts compiled the regex as `^...™?s*(MI)$` instead of `^...™?\s*\(MI\)$` and never matched any dropdown option (15s timeout at game selection while the correct options were visibly listed). Backslashes doubled in all three pattern functions (RTC FS / RAF / SUO). Fix verified end-to-end: each template's pattern function was extracted from the HTML, rendered through a real JS template literal via node, executed in Python, and the compiled regex tested against `Triple Cash Eruption™ (MI)` (match), the pre-update non-™ name (match), and wrong jurisdictions including `(ON)` (reject). v2.20 should not be used — any scripts generated from it will fail at Casino Game selection. |
| v2.20 | Aug 9, 2026 | **Casino Game selection made ™-tolerant (RTC FS / RAF / SUO)** — NATS renamed casino games to include a trademark symbol (e.g. `Triple Cash Eruption` → `Triple Cash Eruption™ (WV)`), which broke game selection in all three free-spin flows: scripts typed `{searchName} ({jurisdiction})` into the search box, and the `™` sitting between the name and the jurisdiction suffix caused zero search results, followed by a 15s exact-match timeout. Game selection now works in two parts: **(1) Search** — scripts type only the portion of the search name before any `™` (`Triple Cash Eruption`, `7's Fire Blitz`, `WrestleMania`), surfacing every variant regardless of how NATS renders the symbol; **(2) Match** — the dropdown option is chosen with an anchored regex where every `™` is optional (both mid-name and trailing) but the jurisdiction suffix is required, so the correct state's game is always selected whether or not NATS includes `™`, and wrong jurisdictions or similarly-named games can never match. `parse_casino_game()` replaced by `parse_casino_game_search()` + `parse_casino_game_pattern()` in the RTC FS, RAF, and SUO templates. Terminal output now prints the actual option text NATS displayed (e.g. `Casino Game: Triple Cash Eruption™ (WV)`). The `GAMES` table is unchanged — no search-name edits are needed for the four existing games, and the matcher is resilient if NATS adds or removes `™` again. Matching logic validated against 10 unit tests covering old naming, new naming, wrong-jurisdiction rejection, and similar-name rejection. |
| v2.19 | Aug 9, 2026 | **Free-spin offer types made currency-aware** — RTC FS, RAF, and SUO Day 2+ spin-value selection updated to match the v2.18 RTC CC pattern. The Free Spin Stakes table row is now chosen dynamically via the `currency_for()` helper (already embedded in every template since v2.18) instead of a hardcoded `USD` label anchor: RTC FS derives currency from the code name; RAF and SUO derive it from the script's `JURISDICTION` constant. US/MI/WV/PA/NJ → **USD**, ON/AB/CA → **CAD**, GBP unreachable. Behavior is unchanged today since all three offer types are restricted to MI/NJ/WV/PA (always USD) — this future-proofs the flows for any Canadian free-spins expansion with no further code change beyond allowing the jurisdiction in the UI. Terminal output now prints the selected currency (e.g. `Spin Value (USD): $0.20`). |
| v2.18 | Aug 9, 2026 | **NATS multi-currency amount tables** — a NATS update added a currency dimension to the bonus amount tables: what was a single amount input is now a three-row table with one row per currency (`USD` / `GBP` / `CAD`), each keyed by `data-row-key` on the `<tr>`. The old page-wide selector (`td.ant-table-cell.center` → `input[aria-valuemin='0']`) now matches 3 elements and crashes with a Playwright strict mode violation (first observed on an RTC CC bonus build, Aug 9). All generated scripts now include a shared `currency_for(name)` / `fill_currency_amount()` helper: jurisdiction is derived from the code name (US/MI/WV/PA/NJ → **USD**; ON/AB/CA → **CAD**; **GBP is never used under any circumstance**) and the amount is filled via `tr[data-row-key='{currency}']`, with a fallback to the legacy single-input selector if NATS ever reverts. Patched fill sites: **RTC CC** Casino Credit amount, **LC-REACT** Casino Credit amount, **BG** FanCash amount, **DM** Bonus amount + Minimum/Maximum Deposit cards, **LC-CHURN-DM** Bonus/Casino Credit amount card + Minimum/Maximum Deposit cards. The DM and LC-CHURN-DM card fills previously used `.first`, which would not have crashed but would have silently filled the **USD row for Canadian jurisdictions** — now fixed. RTC FS / RAF / SUO free-spin fields are label-scoped form items, not amount tables, and are unaffected; their USD-only jurisdictions mean any `.first` card fills still land on the USD row. Percentage inputs (`aria-valuemax='100'`) are not part of the currency table and are unchanged. |
| v2.17 | Jul 27, 2026 | New offer type: **Lifecycle - Churn DM** — ninth offer type added to the HTML tool. Deposit-match offer paid in **Casino Credit** for churn-risk customers. Day card: 4-column grid, 4 days by default; offer rows are **Min → % → Max** with per-offer checkbox — five defaults shown unselected ($10/50%/$10, $20/50%/$25, $20/50%/$50, $50/50%/$100, $50/50%/$200); Segment/Promo/Bonus step badges; Send to XP default ON; External Bonus present, default OFF; Days to Opt In 3 / Entitlement 3 / Settlement 7. No Campaign Name field and no ZIP attachment. Code name format: `MMDDYY_CAS_LC_CHURN_DM_{jurisdiction}_CC_M{min}_{pct}M{max}`. Promo window and bonus activation window: `00:00:00 ET` day-of → `00:00:00 ET` day+10 (exactly 10 days). Voucher code format: `CRLCDM{MMDDYY}{XXX}`. Title, Promo Header Text, and Bonus Description all fixed as `Deposit and Get Casino Credit`; no Button 2 label/link on the promo. Bonus type Casino Credit — percentage checkbox is labeled `Casino Credit (%)` on the CC form (script includes fallbacks between `Bonus %`/`Casino Credit %` summary labels and `Bonus`/`Casino Credit` summary-card titles); settlement field targets `Days to Meet Casino_credit Settlement`; Minimum Deposit dynamic from `M{min}` in the code; Maximum Deposit hardcoded 1000000. Reporting: Platform CAS (Casino), Origin `Retention`, Series `Other`, Type `Lifecycle`. No confirmation modal after Bonus Save. HIW and full legal-approved T&C baked in at generation time with dynamic fields `{min_fmt}` / `{pct}` / `{max_fmt}` / `{terms_id}`; T&C title line: `FANATICS CASINO - DEPOSIT {min_fmt}+, GET {pct}% DEPOSIT MATCH UP TO {max_fmt} IN CC - {terms_id}`; Terms IDs baked in per offer key (`M10_50M10`→`CAS_10620`, `M20_50M25`→`CAS_9457`, `M20_50M50`→`CAS_9458`, `M50_50M100`→`CAS_9459`, `M50_50M200`→`CAS_9460`; shared promotional period June 1, 2026 → December 1, 2026). **Terms ID enforcement:** active offers without a configured Terms ID show a red warning on the offer row and block script generation with an alert. Images pulled from a locally-synced Google Drive folder (RTC-style) — localStorage key `lc_churn_dm_image_path`, default path `.../Lifecycle Automations/Churn Risk DM`, Google Drive folder `https://drive.google.com/drive/folders/1KJfVtJVwBbGkJJTr8T-PGeWHm5c8qc-2`; filenames `Promo Detail.png`, `Masthead.png`, `{pct}M{max}.png` (e.g. `50M10.png` — min deposit not part of the filename). localStorage keys: `lc_churn_dm_hiw`, `lc_churn_dm_tc`, `lc_churn_dm_image_path`. Jurisdictions: US (default), MI, WV, PA, NJ, ON, AB, CA. Generates `lc_churn_dm_MMDDYY_HHMM.py`. All promo and bonus steps validated field-by-field in NATS via staged test scripts before integration. |
| v2.16 | Jul 27, 2026 | New offer type: **Lifecycle - REACT CC Drop** — eighth offer type added to the HTML tool. Day card: 3 days by default, amounts grid shows only `$10`, `$25`, `$50` (all unselected by default), Segment/Promo/Bonus step badges, Send to XP, Days to Opt In / Entitlement / Settlement (all default to 3). Code name format: `MMDDYY_CAS_LC_REACT_{jurisdiction}_CC_{amount}`. Promo window: `00:00:00 ET → 23:59:59 ET +72h`. Bonus activation window: identical to promo window. Voucher code format: `CRLCCC{MMDDYY}{XXX}`. T&C baked in per-amount with per-tier Terms IDs (`CAS_9237` / `CAS_9238` / `CAS_9456`) and per-tier promotional periods. Promo Title and Header Text both read `Get $X in Casino Credit`. Bonus Description: `Get $X in Casino Credit`. Bonus Origin: `Opt-In Bonus`. Series: `Reactivation`. Type: `Lifecycle`. No confirmation modal after Save. Segment dropdown click fixed to use anchored regex scoped to `.ant-select-dropdown:not(.ant-select-dropdown-hidden)` — consistent with Promotion Tile selector pattern. localStorage keys: `lc_react_hiw`, `lc_react_tc`, `lc_react_image_path`. Google Drive images folder: `https://drive.google.com/drive/folders/1Xhk-3fKF3goNS_lL7M0bn2DrACKyFRds`. Generates `lc_react_MMDDYY_HHMM.py`. |
| v2.15 | Jun 23, 2026 | New offer type: **Bet & Get - Rules Engine** — seventh offer type added to the HTML tool; two-phase script: Phase 1 builds NATS promos (identical to existing BG flow including ZIP image upload), Phase 2 builds Playmaker bonuses at `playmaker-internal.test1.fanatics.bet`; day card fields: Date, Images (ZIP), Offers (dynamic bet/get rows), Reward Type dropdown (FanCash default), Jurisdiction dropdown, Days to Use Reward, Show Toast toggle, Send to XP; step badges: Promo + Bonus only (no Segment — segment is built inside Playmaker as part of the bonus flow); code name format `MMDDYY_CAS_RET_BG_{jurisdiction}_{rewardType}_B{bet}_G{get}`; generates `bg_re_MMDDYY_HHMM.py`. ⚠️ NATS and Playmaker URLs are currently pointed at the **test environment** — switch to production URLs once the Rewards Engine is live in the Playmaker production environment. |
| v2.14 | Jun 22, 2026 | New offer type: **SUO Day 2+ Spins** — sixth offer type added to the HTML tool; generates segments and bonuses for DAY2–DAYN codes; single campaign card (Date, Jurisdiction, Game, Spins/Day, Spin Value, Bet Amount, Number of Days toggle); no promos, no voucher codes, no step badges; Trigger = No Opt In, Series = Early Life, Status Active = checked; code name format `MMDDYY_CAS_ACQ_SUO_DAY{n}_{jurisdiction}_FS_{spins}`; generates `suo_day2_MMDDYY_HHMM.py`. |
| v2.13 | Jun 16, 2026 | `create_segment` AMELCO selection timing fix across all five offer types — 500ms wait after `dispatch_event("click")` replaced with a 2-second wait after `#forBonus` check, giving NATS more time to register the Region selection before the OK button is clicked. |
| v2.12 | Jun 15, 2026 | Image upload wait after file selection increased from 5 seconds to 8 seconds across all five offer types (RTC CC, RTC FS, BG, DM, RAF) — `upload_image` `wait_for_timeout` after `set_files` updated in all five script builders. |
| v2.11 | Jun 15, 2026 | RTC CC promo and bonus activation windows changed from midnight-to-midnight to 4 AM–4 AM Eastern — `parse_dates()` and the `create_bonus` activation block now start at 04:00:00 ET on the day-of and end 24 hours later (04:00:00 ET the following day). Change is scoped exclusively to the RTC Top Up - Casino Credit script builder; BG, DM, RTC FS, and RAF are unchanged. |
| v2.10 | Jun 10, 2026 | Platform tag removal updated for all five offer types — scripts previously removed `COMBO: Casino` and `COMBO: Sportsbook`; now remove `COMBO: Sportsbook` and `COMBO: Sportsbook And Casino`, leaving `STAC: Standalone Casino`, `Web`, and `COMBO: Casino` as the active platforms on every bonus. |
| v2.9 | Jun 3, 2026 | AMELCO dropdown fix — `create_segment` was using `.click()` to select AMELCO, which timed out due to an Ant Design `ant-select-item-option-content` div intercepting pointer events; switched to `.dispatch_event("click")` across all five offer types (RTC CC, RTC FS, BG, DM, RAF). RTC CC bonus activation end date changed from day+30 to day+1 (e.g. June 11 → June 12 at 00:00:00 ET); RTC FS activation window unchanged. Create Promotion button poll added to all five `create_promo` functions — button was being clicked immediately after returning to the Promos list before the UI re-enabled it; now polls up to 60s (120 × 500ms) matching the existing Create Bonus pattern. |
| v2.8 | Jun 1, 2026 | Fixed RAF Generate Script button always remaining faded/disabled — `updateGenerateButton()` was missing a RAF branch and falling through to the amounts check which RAF does not use; button now enables when date (6 chars), Spins/Day, Bet Amount, and Spin Value are all filled |
| v2.7 | Jun 1, 2026 | Refer a Friend offer type fully integrated into the HTML tool — campaign card with Date, Jurisdiction, Game, Spins/Day, Spin Value, Bet Amount, Number of Days toggle (5/10), phase badges (Day 2+, Referee, Referrer), and live preview panel; `buildRAFScript()` generates complete `raf_full_campaign_MMDDYY_HHMM.py` with all constants injected from UI; `raf_image_path` localStorage key added; Edit HIW and Edit T&C buttons hidden for RAF; RAF uses same sidebar-based `data-menu-id` navigation as all other offer types |
| v2.6 | Jun 1, 2026 | Sidebar-based navigation replaces dashboard navigation in all four generated script types — scripts now hover sidebar icons by `data-menu-id` attribute and click the target submenu item directly, removing dependency on the dashboard that not all users have access to; `Expand All` step removed from all scripts; submenu clicks use `dispatch_event("click")` to prevent timeout errors; retry loop (5 attempts) with safe-zone mouse reset added to all three nav steps |
| v2.5 | May 26, 2026 | Mac Setup Guide published to GitHub Pages; setup guide link added to tool info panel; run cards collapsed to one combined `&&` command per platform |
| v2.4 | May 22, 2026 | Eastern Time enforcement via `zoneinfo` across all four generated script types |
| v2.3 | May 22, 2026 | New 15-character voucher code format with random 3-character runtime suffix; RTC FS confirmation modal handling corrected (no modal) |
| v2.1 | May 21, 2026 | RTC CC Terms IDs & promotional date fields in T&C; `hiwText is not defined` fix for RTC FS |
| v2.0 | May 21, 2026 | Two new RTC FS games added (7FH, 7P5) |
| v1.9.5c | May 21, 2026 | RTC FS offer type added; per-code step toggles (Segment / Promo / Bonus) on all day cards; Days to Settlement field added for BG and DM; various bug fixes |
| v1.8 | — | RTC FS offer type (initial); RTC CC renamed from "RTC Top Up" to "RTC Top Up - Casino Credit" |
| v1.7 | May 8, 2026 | BG switched to per-day ZIP image attachment; Promotion Tile regex exact match; Create Bonus button poll extended to 60s; macOS ZIP metadata stripping |
