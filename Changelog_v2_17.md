# NATS Bonus Creator — Changelog

| Version | Date | Summary |
|---|---|---|
| v2.17 | Jul 27, 2026 | New offer type: **Lifecycle - Churn DM** — ninth offer type added to the HTML tool. Deposit-match offer paid in **Casino Credit** for churn-risk customers. Day card: 4-column grid, 4 days by default; offer rows are **Min → % → Max** with per-offer checkbox — five defaults shown unselected ($10/50%/$10, $20/50%/$25, $20/50%/$50, $50/50%/$100, $50/50%/$200); Segment/Promo/Bonus step badges; Send to XP default ON; External Bonus present, default OFF; Days to Opt In 3 / Entitlement 3 / Settlement 7. No Campaign Name field and no ZIP attachment. Code name format: `MMDDYY_CAS_LC_CHURN_DM_{jurisdiction}_CC_M{min}_{pct}M{max}`. Promo window and bonus activation window: `00:00:00 ET` day-of → `00:00:00 ET` day+10 (exactly 10 days). Voucher code format: `CRLCDM{MMDDYY}{XXX}`. Title, Promo Header Text, and Bonus Description all fixed as `Deposit and Get Casino Credit`; no Button 2 label/link on the promo. Bonus type Casino Credit — percentage checkbox is labeled `Casino Credit (%)` on the CC form (script includes fallbacks between `Bonus %`/`Casino Credit %` summary labels and `Bonus`/`Casino Credit` summary-card titles); settlement field targets `Days to Meet Casino_credit Settlement`; Minimum Deposit dynamic from `M{min}` in the code; Maximum Deposit hardcoded 1000000. Reporting: Platform CAS (Casino), Origin `Retention`, Series `Other`, Type `Lifecycle`. No confirmation modal after Bonus Save. HIW and full legal-approved T&C baked in at generation time with dynamic fields `{min_fmt}` / `{pct}` / `{max_fmt}` / `{terms_id}`; T&C title line: `FANATICS CASINO - DEPOSIT {min_fmt}+, GET {pct}% DEPOSIT MATCH UP TO {max_fmt} IN CC - {terms_id}`; Terms IDs baked in per offer key (`M10_50M10`→`CAS_10620`, `M20_50M25`→`CAS_9457`, `M20_50M50`→`CAS_9458`, `M50_50M100`→`CAS_9459`, `M50_50M200`→`CAS_9460`; shared promotional period June 1, 2026 → December 1, 2026). **Terms ID enforcement:** active offers without a configured Terms ID show a red warning on the offer row and block script generation with an alert. Images pulled from a locally-synced Google Drive folder (RTC-style) — localStorage key `lc_churn_dm_image_path`, default path `.../Lifecycle Automations/Churn Risk DM`, Google Drive folder `https://drive.google.com/drive/folders/1KJfVtJVwBbGkJJTr8T-PGeWHm5c8qc-2`; filenames `Promo Detail.png`, `Masthead.png`, `{pct}M{max}.png` (e.g. `50M10.png` — min deposit not part of the filename). localStorage keys: `lc_churn_dm_hiw`, `lc_churn_dm_tc`, `lc_churn_dm_image_path`. Jurisdictions: US (default), MI, WV, PA, NJ, ON, AB, CA. Generates `lc_churn_dm_MMDDYY_HHMM.py`. All promo and bonus steps validated field-by-field in NATS via staged test scripts before integration. |
| v2.16 | Jul 27, 2026 | New offer type: **Lifecycle - REACT CC Drop** — eighth offer type added to the HTML tool. Day card: 3 days by default, amounts grid shows only `$10`, `$25`, `$50` (all unselected by default), Segment/Promo/Bonus step badges, Send to XP, Days to Opt In / Entitlement / Settlement (all default to 3). Code name format: `MMDDYY_CAS_LC_REACT_{jurisdiction}_CC_{amount}`. Promo window: `00:00:00 ET → 23:59:59 ET +72h`. Bonus activation window: identical to promo window. Voucher code format: `CRLCCC{MMDDYY}{XXX}`. T&C baked in per-amount with per-tier Terms IDs (`CAS_9237` / `CAS_9238` / `CAS_9456`) and per-tier promotional periods. Promo Title and Header Text both read `Get $X in Casino Credit`. Bonus Description: `Get $X in Casino Credit`. Bonus Origin: `Opt-In Bonus`. Series: `Reactivation`. Type: `Lifecycle`. No confirmation modal after Save. Segment dropdown click fixed to use anchored regex scoped to `.ant-select-dropdown:not(.ant-select-dropdown-hidden)` — consistent with Promotion Tile selector pattern. localStorage keys: `lc_react_hiw`, `lc_react_tc`, `lc_react_image_path`. Google Drive images folder: `https://drive.google.com/drive/folders/1Xhk-3fKF3goNS_lL7M0bn2DrACKyFRds`. Generates `lc_react_MMDDYY_HHMM.py`. |
| v2.15 | Jun 23, 2026 | New offer type: **Bet & Get - Rules Engine** — seventh offer type added to the HTML tool; two-phase script: Phase 1 builds NATS promos (identical to existing BG flow including ZIP image upload), Phase 2 builds Playmaker bonuses at `playmaker-internal.test1.fanatics.bet`; day card fields: Date, Images (ZIP), Offers (dynamic bet/get rows), Reward Type dropdown (FanCash default), Jurisdiction dropdown, Days to Use Reward, Show Toast toggle, Send to XP; step badges: Promo + Bonus only (no Segment — segment is built inside Playmaker as part of the bonus flow); code name format `MMDDYY_CAS_RET_BG_{jurisdiction}_{rewardType}_B{bet}_G{get}`; generates `bg_re_MMDDYY_HHMM.py`. ⚠️ NATS and Playmaker URLs are currently pointed at the **test environment** — switch to production URLs once the Rewards Engine is live in the Playmaker production environment. |
| v2.14 | Jun 22, 2026 | New offer type: **SUO Day 2+ Spins** — sixth offer type added to the HTML tool; generates segments and bonuses for DAY2–DAYN codes; single campaign card (Date, Jurisdiction, Game, Spins/Day, Spin Value, Bet Amount, Number of Days toggle); no promos, no voucher codes, no step badges; Trigger = No Opt In, Series = Early Life, Status Active = checked; code name format `MMDDYY_CAS_ACQ_SUO_DAY{n}_{jurisdiction}_FS_{spins}`; generates `suo_day2_MMDDYY_HHMM.py`. |
| v2.13 | Jun 16, 2026 | `create_segment` AMELCO selection timing fix across all five offer types — 500ms wait after `dispatch_event("click")` replaced with a 2-second wait after `#forBonus` check, giving NATS more time to register the Region selection before the OK button is clicked. |
| v2.12 | Jun 15, 2026 | Image upload wait after file selection increased from 5 seconds to 8 seconds across all five offer types (RTC CC, RTC FS, BG, DM, RAF) — `upload_image` `wait_for_timeout` after `set_files` updated in all five script builders. |
| v2.11 | Jun 15, 2026 | RTC CC promo and bonus activation windows changed from midnight-to-midnight to 4 AM–4 AM Eastern — `parse_dates()` and the `create_bonus` activation block now start at 04:00:00 ET on the day-of and end 24 hours later (04:00:00 ET the following day). Change is scoped exclusively to the RTC Top Up - Casino Credit script builder; BG, DM, RTC FS, and RAF are unchanged. |
| v2.10 | Jun 10, 2026 | Platform tag removal updated for all five offer types — scripts previously removed `COMBO: Casino` and `COMBO: Sportsbook`; now remove `COMBO: Sportsbook` and `COMBO: Sportsbook And Casino`, leaving `STAC: Standalone Casino`, `Web`, and `COMBO: Casino` as the active platforms on every bonus. |
| v2.9 | Jun 3, 2026 | AMELCO dropdown fix — `create_segment` was using `.click()` to select AMELCO, which timed out due to an Ant Design `ant-select-item-option-content` div intercepting pointer events; switched to `.dispatch_event("click")` across all five offer types (RTC CC, RTC FS, BG, DM, RAF). RTC CC bonus activation end date changed from day+30 to day+1 (e.g. June 11 → June 12 at 00:00:00 ET); RTC FS activation window unchanged. Create Promotion button poll added to all five `create_promo` functions — button was being clicked immediately after returning to the Promos list before the UI re-enabled it; now polls up to 60s (120 × 500ms) matching the existing Create Bonus pattern. |
| v2.8 | Jun 1, 2026 | Fixed RAF Generate Script button always remaining faded/disabled — `updateGenerateButton()` was missing a RAF branch and falling through to the amounts check which RAF does not use; button now enables when date (6 chars), Spins/Day, Bet Amount, and Spin Value are all filled |
| v2.7 | Jun 1, 2026 | Refer a Friend offer type fully integrated into the HTML tool — campaign card with Date, Jurisdiction, Game, Spins/Day, Spin Value, Bet Amount, Number of Days toggle (5/10), phase badges (Day 2+, Referee, Referrer), and live preview panel; `buildRAFScript()` generates complete `raf_full_campaign_MMDDYY_HHMM.py` with all constants injected from UI; `raf_image_path` localStorage key added; Edit HIW and Edit T&C buttons hidden for RAF; RAF uses same sidebar-based `data-menu-id` navigation as all other offer types |
| v2.6 | Jun 1, 2026 | Sidebar-based navigation replaces dashboard navigation in all four generated script types — scripts now hover sidebar icons by `data-menu-id` attribute and click the target submenu item directly, removing dependency on the dashboard that not all users have access to; `Expand All` step removed from all scripts; submenu clicks use `dispatch_event("click")` to prevent timeout errors; retry loop (5 attempts) with safe-zone mouse reset added to all three nav steps |
| v2.5 | May 26, 2026 | Mac Setup Guide published to GitHub Pages; setup guide link added to tool info panel; run cards collapsed to one combined `&&` command per platform |
| v2.4 | May 22, 2026 | Eastern Time enforcement via `zoneinfo` across all four generated script types |
| v2.3 | May 22, 2026 | New 15-character voucher code format with random 3-character runtime suffix; RTC FS confirmation modal handling corrected (no modal) |
| v2.1 | May 21, 2026 | RTC CC Terms IDs & promotional date fields in T&C; `hiwText is not defined` fix for RTC FS |
| v2.0 | May 21, 2026 | Two new RTC FS games added (7FH, 7P5) |
| v1.9.5c | May 21, 2026 | RTC FS offer type added; per-code step toggles (Segment / Promo / Bonus) on all day cards; Days to Settlement field added for BG and DM; various bug fixes |
| v1.8 | — | RTC FS offer type (initial); RTC CC renamed from "RTC Top Up" to "RTC Top Up - Casino Credit" |
| v1.7 | May 8, 2026 | BG switched to per-day ZIP image attachment; Promotion Tile regex exact match; Create Bonus button poll extended to 60s; macOS ZIP metadata stripping |
